# Copy to .env and fill in the values you use locally.
# This file is not loaded automatically; .env is gitignored.

# Option A: Google AI Studio API key (recommended for quickstart)
# Obtain from Google AI Studio / Vertex AI Express Mode.
GOOGLE_API_KEY=

# Option B: Vertex AI settings (if using Vertex backend)
# Ensure Application Default Credentials are configured, or set a service account key path.
VERTEXAI_PROJECT=
VERTEXAI_LOCATION=us-central1
# Path to a service account key JSON (optional if using ADC)
GOOGLE_APPLICATION_CREDENTIALS=

# Notion MCP (SSE) token for Authorization header
# Example: NOTION_MCP_TOKEN=secret_xxx
NOTION_MCP_TOKEN=

# Default Notion database ID used when not provided explicitly to tools
# Example: NOTION_DATABASE_ID=2554cc35-4636-8145-aba9-c56e5b103977
NOTION_DATABASE_ID=

# Tavily API key for web search (preferred)
# Obtain from https://docs.tavily.com/ — used by local MCP tool tavily_search
TAVILY_API_KEY=

# Firecrawl MCP API key (optional; kept for backward compatibility)
# Example: FIRECRAWL_API_KEY=fc-xxxxxxx
FIRECRAWL_API_KEY=
