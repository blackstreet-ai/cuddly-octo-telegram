# Copy to .env and fill in the values you use locally.
# This file is not loaded automatically; .env is gitignored.

# Option A: Google AI Studio API key (recommended for quickstart)
# Obtain from Google AI Studio / Vertex AI Express Mode.
GOOGLE_API_KEY=

# Option B: Vertex AI settings (if using Vertex backend)
# Ensure Application Default Credentials are configured, or set a service account key path.
VERTEXAI_PROJECT=
VERTEXAI_LOCATION=us-central1
# Path to a service account key JSON (optional if using ADC)
GOOGLE_APPLICATION_CREDENTIALS=
